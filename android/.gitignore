gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# nulleig-tv.jks in particular: the TV release key. The workflow writes it here
# from the TV_KEYSTORE_BASE64 secret at build time, so on a runner this path is
# a real file that must not be committed back.
